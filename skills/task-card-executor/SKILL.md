# task-card-executor

## Goal

读取任务卡并按固定开发纪律推进到完成。

## Workflow

1. 读取 `CLAUDE.md`
2. 读取本次任务卡
3. 先输出 `plan`
4. 先更新设计文档和测试文档
5. 再进行实现
6. 完成后执行验证
7. 更新 `MEMORY.md`
8. 最后汇报结果

## Rules

- 不允许跳过 `plan`
- 不允许跳过设计文档和测试文档更新
- 页面任务必须实际验证
- 若实现中设计变化，先同步更新文档再继续
- 除非出现真实阻塞，否则不中断任务
